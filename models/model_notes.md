# Patient Outcome Model Notes

Model: RandomForestClassifier  
Trained on: ICU Data (cleaned)  
Features: age, heart_rate, BP, comorbidities  
Targets: survival (binary), length_of_stay (numeric), complications (multi-class)

Next Steps:
- Try XGBoost
- Tune hyperparameters
